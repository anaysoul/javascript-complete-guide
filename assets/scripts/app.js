const addMovieModal = document.getElementById('add-modal');

const startAddMovieButton = document.querySelector('header button');

const addModal = (addMovieModal) => {
  addMovieModal.className = 'modal visible';
};
